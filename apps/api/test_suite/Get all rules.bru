meta {
  name: Get all rules
  type: http
  seq: 2
}

get {
  url: http://localhost:4400/v1/context1/rules
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  x-api-key: {{BREASE_API_KEY}}
  x-org-id: {{BREASE_ORG_ID}}
}

tests {
  test("OK", function() {
    expect(res.status).to.equal(200);
  });
    
  test("should not have any rules yet", function() {
    expect(res.body).to.be.an('object')
    expect(res.body.rules).to.equal(null);
  });
}
